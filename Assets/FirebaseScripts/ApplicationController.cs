using UnityEngine;
using UniRx;
using Firebase.Auth;

public class ApplicationController : MonoBehaviour
{
    private void Awake()
    {
        // Removed automatic test user creation to prevent duplicate players
    }
}
